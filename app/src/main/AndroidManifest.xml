<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kaleb.truestrength">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HelpActivity" />
        <activity android:name=".ProfileScreen" />
        <activity android:name=".Test" />
        <activity android:name=".TrueStrength" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".CreateProfileActivity" />
        <activity android:name=".exerciseList" />
        <activity android:name=".firstList" />
        <activity android:name=".myExerciseActivity" />
        <activity android:name=".repsets" />
        <activity android:name=".secondList" />
        <activity android:name=".timer"></activity>
    </application>

</manifest>